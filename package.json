{
  "name": "file-atlas",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "check:ui": "node --check ui/app.js && node ui/smoke-test.cjs"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.8.4"
  }
}
