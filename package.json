{
  "name": "file-atlas",
  "version": "2.0.0",
  "description": "A local-first visual disk bloat and exact duplicate analyzer.",
  "type": "module",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "dev": "electron .",
    "test": "node --test tests/scanner.test.mjs",
    "check": "node --check main.js && node --check src/scanner.js && node --check src/analysis.js && node --check renderer.js"
  },
  "dependencies": {
    "d3": "^7.9.0"
  },
  "devDependencies": {
    "electron": "^28.0.0"
  }
}
